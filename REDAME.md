# Console Action RPG (C++)

ゲームエンジン（UnityやUnreal Engineなど）に一切頼らず、C++とOSの低レイヤーAPIのみを用いてフルスクラッチで開発した、コンソール上で動くアクションコマンドRPGです。

## 💡 コンセプトと開発の目的
本プロジェクトの最大の目的は、**「プログラミングにおけるブラックボックスの排除」**です。
便利なフレームワークやエンジンが隠蔽している「メモリ管理」「データ構造」「OSとの非同期な対話（システムコール）」といった根幹のロジックを、自らの手でC++を用いてゼロから構築・制御することを目指しました。

## ✨ コア機能と技術的ハイライト

### 1. OSの壁を越えた「リアルタイム・パリィシステム」
通常のターン制RPG（同期処理）の中に、敵の攻撃アクション（非同期処理）をシームレスに融合させています。
Linux環境の `termios.h` および `fcntl.h` を操作してターミナルの設定をオーバーライドし、標準入力のブロッキング（エンターキー待ち）を強制解除。これにより、コンソール画面でありながら「リアルタイムに時間をカウントダウンし、特定のフレーム間のみ入力を受け付ける」というゲームループの心臓部を自力で実装しました。

### 2. 「MP・回復魔法」の廃止と、リスク＆リターン型の戦闘ロジック
旧来の「リソースを消費して回復する」という作業的なバトルシステムを見直し、よりアクション性とプレイヤーのスキルに依存するシステムへ再構築しました。
敵の攻撃到達直前の約0.4秒間に `[p]` キーを入力してパリィ（ジャストガード）を成功させることで、ダメージを無効化しつつ自身のHPが回復するロジックを採用しています。

### 3. 堅牢な状態管理とメモリアクセス
* `std::vector` を活用した、人数変動に強い動的なパーティ（味方・敵）のデータ管理。
* 「死体蹴り」を防ぐ矛盾のないターゲット選択AIロジック。
* 参照渡し（`&`）を用いた、不必要なインスタンスのコピーを防ぐ厳密なステータス変更フロー。

## 🛠 使用技術
* **言語:** C++ (標準ライブラリのみ使用)
* **環境:** Linux (Ubuntu / WSL)
* **バージョン管理:** Git / GitHub

## 🚀 遊び方・実行手順

Linux環境（WSL等）のターミナルで以下のコマンドを実行してください。

```bash
# リポジトリのクローン
git clone [https://github.com/あなたのユーザー名/あなたのレポジトリ名.git](https://github.com/あなたのユーザー名/あなたのレポジトリ名.git)
cd あなたのレポジトリ名

# コンパイル
g++ mini_rpg.cpp -o mini_rpg

# 実行
./mini_rpg